ファイル書き込みの許可が必要です。許可をいただければ、以下の変更を適用します：

**変更内容：**
1. `ClaudeProviderOptions` に `maxRetries`（デフォルト: 3）と `baseRetryDelay`（デフォルト: 1000ms）を追加
2. `isRetryableError()` メソッドでタイムアウト、レートリミット、503、429エラーを検出
3. `runClaudeOnce()` に元の実行ロジックを移動
4. `runClaude()` で指数バックオフ付きリトライ（1秒 → 2秒 → 4秒）を実装

許可をいただければ書き込みを行います。
[<u[?1004l[?2004l[?25h[?25h